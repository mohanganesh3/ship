# ── Core HTTP & Scraping ──────────────────────────────────────────────────────
requests>=2.31.0
requests-cache>=1.1.1
urllib3>=2.0.0
beautifulsoup4>=4.12.0
lxml>=5.2.0
playwright>=1.44.0          # for JS-rendered pages (NTSB)
crawlee>=0.3.0              # Apify's Python crawler

# ── PDF & EPUB Extraction ─────────────────────────────────────────────────────
marker-pdf>=0.2.14          # primary: best quality, GPU optional
PyMuPDF>=1.24.0             # fallback: speed-first (fitz)
pdfplumber>=0.11.0          # table extraction fallback
EbookLib>=0.18              # EPUB reading
html2text>=2024.2.26        # HTML→plain text (used by EPUB extractor)

# ── Text Cleaning & Language Detection ───────────────────────────────────────
ftfy>=6.2.0
langdetect>=1.0.9
fasttext-wheel>=0.9.2       # faster lang-id; or install from HF mirror
chardet>=5.2.0

# ── Deduplication ─────────────────────────────────────────────────────────────
datasketch>=1.6.5           # MinHash LSH
xxhash>=3.4.1               # fast exact hashing

# ── Quality Filtering ─────────────────────────────────────────────────────────
# kenlm is C++ - install separately:  pip install https://github.com/kpu/kenlm/...
# We include a pure-Python heuristic fallback in filtering/quality_filter.py

# ── Dataset & Storage ─────────────────────────────────────────────────────────
datasets>=2.19.0            # HuggingFace datasets
pyarrow>=16.0.0
pandas>=2.2.0

# ── Wikipedia ────────────────────────────────────────────────────────────────
wikipedia-api>=0.6.0
mwparserfromhell>=0.6.6     # clean MediaWiki markup

# ── Utilities ────────────────────────────────────────────────────────────────
tqdm>=4.66.0
tenacity>=8.3.0             # retry logic
click>=8.1.0                # CLI
rich>=13.7.0                # pretty logging
schedule>=1.2.0
python-dotenv>=1.0.0
robotsparser>=0.0.1         # robots.txt respect (stdlib urllib.robotparser is fine too)
